# Server configuration
server.port=8080

# Database configuration
spring.datasource.driver-class-name=org.postgresql.Driver

# === Datasource ===
spring.datasource.url=jdbc:postgresql://localhost:5432/merchdb
spring.datasource.username=merch
spring.datasource.password=merch


# === JPA / Hibernate ===
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


# Logging configuration
logging.level.org.springframework=INFO
logging.level.com.reinodehades.merchmanager=DEBUG
# Log detallado de autenticación
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.security.web.authentication=DEBUG
